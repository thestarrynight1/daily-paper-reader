---
title: Physics-informed post-processing of stabilized finite element solutions for transient convection-dominated problems
title_zh: 针对瞬态对流占优问题的稳定化有限元解的物理信息后处理
authors: "Süleyman Cengizci, Ömür Uğur, Srinivasan Natesan"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03259v1"
tags: ["query:wind-wind-control"]
score: 8.0
evidence: 扩展PINN用于瞬态输运的混合框架
tldr: 针对对流占导的瞬态输运问题中存在的数值振荡和陡峭梯度捕捉难点，本文提出一种物理信息增强的混合计算框架。该方法将半离散稳定化有限元法（SUPG+YZbeta）与PINN后处理策略相结合，通过在终端时刻附近利用时空残差约束对有限元解进行修正。实验证明，该框架在处理边界层、行波及非线性Burgers方程时，显著提升了终端时刻的数值精度。
selection_source: fresh_fetch
motivation: 传统稳定化有限元法在处理强对流瞬态问题时仍存在局部精度不足，而独立PINN难以捕捉尖锐解结构且训练成本高。
method: 提出一种将SUPG稳定化有限元与PINN后处理相结合的混合框架，利用随机傅里叶特征和自适应权重在终端时刻附近进行局部修正。
result: 在包括边界层、行波和非线性Burgers方程在内的五个基准测试中，该方法在终端时刻的精度显著优于传统的稳定化有限元解。
conclusion: 结合物理信息的后处理策略能有效弥补有限元法的局部误差，为解决复杂的对流占导瞬态问题提供了一种高效且高精度的混合方案。
---

## 摘要
对流占优瞬态输运现象的数值模拟由于时空域中存在的剧烈梯度和传播锋面，面临着巨大的计算挑战。经典的离散化方法通常会产生伪振荡，因此需要先进的稳定化技术。然而，即使是稳定化有限元方法，也可能需要额外的正则化来准确解析局部陡峭层。另一方面，独立的物理信息神经网络 (PINNs) 在对流占优状态下难以捕捉剧烈的解结构，且通常需要大量的训练轮数。本研究提出了一种混合计算框架，将带有激波捕捉的 PINN 增强型 SUPG (PASSC) 方法从稳态问题扩展到非稳态问题。该方法结合了半离散稳定化有限元方法和基于 PINN 的修正策略，用于求解瞬态对流-扩散-反应方程。稳定化是通过使用增强了 YZbeta 激波捕捉算子的流线向上 Petrov-Galerkin (SUPG) 公式实现的。神经网络并非在整个时空域上进行训练，而是有选择性地应用于终端时间附近，利用最后 K_s 个时间快照增强有限元解，同时强制执行来自控制方程和边界条件的残差约束。该网络结合了具有随机傅里叶特征的残差块，并采用了带有自适应损失权重的渐进式训练。对五个基准问题（包括边界层和内部层、行波以及非线性 Burgers 动力学）的数值实验表明，与独立的稳定化有限元解相比，该方法在终端时间的准确性有显著提高。

## Abstract
The numerical simulation of convection-dominated transient transport phenomena poses significant computational challenges due to sharp gradients and propagating fronts across the spatiotemporal domain. Classical discretization methods often generate spurious oscillations, requiring advanced stabilization techniques. However, even stabilized finite element methods may require additional regularization to accurately resolve localized steep layers. On the other hand, standalone physics-informed neural networks (PINNs) struggle to capture sharp solution structures in convection-dominated regimes and typically require a large number of training epochs. This work presents a hybrid computational framework that extends the PINN-Augmented SUPG with Shock-Capturing (PASSC) methodology from steady to unsteady problems. The approach combines a semi-discrete stabilized finite element method with a PINN-based correction strategy for transient convection-diffusion-reaction equations. Stabilization is achieved using the Streamline-Upwind Petrov-Galerkin (SUPG) formulation augmented with a YZbeta shock-capturing operator. Rather than training over the entire space-time domain, the neural network is applied selectively near the terminal time, enhancing the finite element solution using the last K_s temporal snapshots while enforcing residual constraints from the governing equations and boundary conditions. The network incorporates residual blocks with random Fourier features and employs progressive training with adaptive loss weighting. Numerical experiments on five benchmark problems, including boundary and interior layers, traveling waves, and nonlinear Burgers dynamics, demonstrate significant accuracy improvements at the terminal time compared to standalone stabilized finite element solutions.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **PASSC**（PINN-Augmented SUPG with Shock-Capturing）的混合计算框架，旨在解决瞬态对流占优（Transient Convection-Dominated）问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在模拟对流占优的瞬态输运现象（如流体力学中的热量或质量传输）时，数值解极易在剧烈梯度（边界层、内部层或激波）附近产生非物理的伪振荡。
*   **研究动机**：
    *   **传统有限元（FEM）的局限**：标准 Galerkin 方法会产生振荡；虽然 SUPG 等稳定化方法有所改善，但在极高对流强度下仍难以完美捕捉尖锐的解结构，且稳定化参数的选取往往依赖经验。
    *   **物理信息神经网络（PINN）的局限**：独立 PINN 在处理强对流问题时收敛极慢，且难以从零开始学习到极薄的边界层结构。
    *   **混合需求**：结合 FEM 的数值稳定性和 PINN 的非线性表征能力，通过“后处理”方式提升终端时刻解的精度。

### 2. 方法论：PASSC 混合框架
*   **核心思想**：先利用带有激波捕捉（Shock-Capturing）的稳定化有限元法生成初步解，再将其作为“先验知识”输入 PINN，在终端时刻附近进行局部的物理信息修正。
*   **关键技术细节**：
    1.  **稳定化 FEM 阶段**：采用 **SUPG**（流线向上 Petrov-Galerkin）公式，并引入 **YZ$\beta$ 激波捕捉算子**。该算子通过残差相关的各向同性人工扩散来抑制局部振荡。
    2.  **PINN 架构**：
        *   **随机傅里叶特征（RFF）**：将时空坐标映射到高维空间，克服神经网络的“光谱偏差”，使其能捕捉高频（尖锐）的解结构。
        *   **残差块（Residual Blocks）**：采用深度残差网络结构，配合 SiLU 激活函数和层归一化（LayerNorm）。
    3.  **选择性物理强化（Selective Physics Enforcement）**：这是本文的一大创新。PINN 的 PDE 残差损失仅在远离边界的内部区域计算（通过距离函数掩码实现），以避免边界层处剧烈梯度对训练稳定性的干扰。
    4.  **多阶段自适应训练**：将训练分为数据主导、过渡、物理主导三个阶段，动态调整数据损失（$L_{data}$）、PDE 残差损失（$L_{pde}$）和边界损失（$L_{bc}$）的权重。

### 3. 实验设计
*   **测试场景（5个基准问题）**：
    1.  **1D 抛物线方程**：具有指数级边界层（$\epsilon = 10^{-4}$）。
    2.  **2D 变高驼峰问题**：具有极薄的内部层（$\epsilon = 10^{-6}$），传统方法易产生条纹伪影。
    3.  **2D 行波问题**：极小扩散系数（$\epsilon = 10^{-8}$）下的移动锋面。
    4.  **2D 非线性 Burgers 方程**：高雷诺数（$Re = 10^4$）下的非线性激波演化。
    5.  **2D L型内部层问题**：无解析解，测试复杂几何形状下的鲁棒性。
*   **对比基准（Benchmark）**：解析解（如有）、标准 SUPG 方案、SUPG-YZ$\beta$ 方案。

### 4. 资源与算力
*   **硬件环境**：Intel Core Ultra 9 275HX 处理器（24核），**NVIDIA GeForce RTX 5070 GPU (8 GB VRAM)**。
*   **软件环境**：Ubuntu 24.04, CUDA 13.0, **FEniCS**（用于 FEM 计算）, **PyTorch**（用于 PINN 训练）。
*   **训练时长**：每个案例通常训练 5000 个 Epoch。虽然未给出具体总时长，但提到使用了混合精度训练（AMP）和 GPU 加速来优化性能。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 5 组深度实验，涵盖了线性/非线性、1D/2D、有/无解析解等多种维度。
*   **充分性评价**：实验设计非常充分。作者不仅展示了 3D 表面图和等高线图，还提供了关键位置的 1D 截面对比图、$L^2$ 误差随时间的演化曲线以及训练损失收敛曲线。
*   **客观性**：通过设置极小的扩散系数（低至 $10^{-8}$）挑战算法极限，并诚实地展示了在某些复杂案例中 PINN 仍存在的微小欠调（undershoot）现象，表现出较高的学术客观性。

### 6. 主要结论
*   **精度提升**：在所有案例中，PASSC 框架在终端时刻的精度均显著优于纯稳定化 FEM。在 Burgers 方程案例中，误差降低了近三个数量级。
*   **振荡抑制**：PINN 后处理能有效消除 SUPG-YZ$\beta$ 仍可能残余的局部振荡或条纹伪影。
*   **物理一致性**：通过引入 PDE 残差约束，PINN 修正后的解比单纯的数据插值更符合物理规律，且能保持更好的解的锐度。

### 7. 优点与亮点
*   **混合范式**：成功解决了 PINN 难以处理强对流和 FEM 难以处理局部极值的问题，利用 FEM 的稳定性为 PINN 提供高质量起点。
*   **选择性强化策略**：通过距离掩码避开边界层干扰，解决了 PINN 在边界附近残差过大导致训练失效的痛点。
*   **架构先进**：引入随机傅里叶特征和残差块，显著增强了网络对多尺度解结构的表达能力。

### 8. 不足与局限
*   **后处理局限性**：该方法目前主要针对终端时刻（Terminal Time）进行修正，而非全时域的替代方案。
*   **超参数敏感性**：多阶段训练的权重（$w_{data}, w_{pde}$）和相位切换点目前仍需根据具体问题进行经验性调整，缺乏通用的自动优化机制。
*   **计算开销**：虽然提升了精度，但相比纯 FEM，增加了一个深度神经网络的训练过程，对于实时性要求极高的场景可能存在挑战。
*   **维度扩展**：虽然理论上支持 3D，但文中实验仅展示到 2D，3D 场景下的显存压力和采样效率尚待验证。

（完）

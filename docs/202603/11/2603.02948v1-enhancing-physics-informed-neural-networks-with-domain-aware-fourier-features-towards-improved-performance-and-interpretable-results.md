---
title: "Enhancing Physics-Informed Neural Networks with Domain-aware Fourier Features: Towards Improved Performance and Interpretable Results"
title_zh: 利用领域感知傅里叶特征增强物理信息神经网络：迈向性能提升与可解释结果
authors: "Alberto Miño Calero, Luis Salamanca, Konstantinos E. Tatsis"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02948v1"
tags: ["query:wind-wind-control"]
score: 9.0
evidence: 利用领域感知特征增强PINN的时空建模能力
tldr: 针对物理信息神经网络（PINNs）训练困难且解释性差的问题，本文提出了一种领域感知傅里叶特征（DaFFs）建模方法。该方法通过将几何形状和边界条件等领域特征封装进位置编码，消除了对显式边界条件损失项的需求，简化了优化过程。结合量身定制的LRP解释性框架，PINN-DaFFs在精度、收敛速度和物理一致性解释方面均显著优于传统PINNs和基于随机傅里叶特征的方法。
selection_source: fresh_fetch
motivation: 解决传统PINN在处理复杂边界条件时存在的训练困难、损失平衡复杂以及模型缺乏可解释性的挑战。
method: 提出领域感知傅里叶特征（DaFFs）进行输入空间编码，并开发了基于逐层相关性传播（LRP）的PINN解释性框架。
result: 实验表明PINN-DaFFs的误差比传统方法低几个数量级，收敛更快，且其特征归因结果更符合物理规律。
conclusion: DaFFs不仅提升了PINN的计算效率和精度，还增强了模型的可解释性，为构建更鲁棒的物理信息学习模型奠定了基础。
---

## 摘要
物理信息神经网络（PINNs）通过将偏微分方程（PDEs）嵌入损失函数，将物理规律融入神经网络中。尽管 PINN 模型在学习底层物理规律方面取得了成功，但其训练和解释仍然具有挑战性。本研究提出了一种新颖的建模方法，该方法依赖于使用领域感知傅里叶特征（DaFFs）对输入空间进行位置编码。这些特征封装了所有特定领域的特性（如几何形状和边界条件），且与随机傅里叶特征（RFFs）不同，它们消除了对显式边界条件损失项和损失平衡方案的需求，同时简化了优化过程并降低了与训练相关的计算成本。我们进一步开发了一个专为 PINNs 定制的基于分层相关性传播（LRP）的可解释性框架，能够提取输入空间的相关性归因分数。结果表明，与原始 PINNs 和基于 RFFs 的 PINNs 相比，PINN-DaFFs 实现了数量级更低的误差，并允许更快的收敛。此外，LRP 分析显示，所提方法带来了更符合物理一致性的特征归因，而 PINN-RFFs 和原始 PINNs 则表现出更分散且物理相关性较低的模式。这些结果证明，DaFFs 不仅增强了 PINNs 的准确性和效率，还提高了可解释性，为更稳健且信息丰富的物理信息学习奠定了基础。

## Abstract
Physics-Informed Neural Networks (PINNs) incorporate physics into neural networks by embedding partial differential equations (PDEs) into their loss function. Despite their success in learning the underlying physics, PINN models remain difficult to train and interpret. In this work, a novel modeling approach is proposed, which relies on the use of Domain-aware Fourier Features (DaFFs) for the positional encoding of the input space. These features encapsulate all the domain-specific characteristics, such as the geometry and boundary conditions, and unlike Random Fourier Features (RFFs), eliminate the need for explicit boundary condition loss terms and loss balancing schemes, while simplifying the optimization process and reducing the computational cost associated with training. We further develop an LRP-based explainability framework tailored to PINNs, enabling the extraction of relevance attribution scores for the input space. It is demonstrated that PINN-DaFFs achieve orders-of-magnitude lower errors and allow faster convergence compared to vanilla PINNs and RFFs-based PINNs. Furthermore, LRP analysis reveals that the proposed leads to more physically consistent feature attributions, while PINN-RFFs and vanilla PINNs display more scattered and less physics-relevant patterns. These results demonstrate that DaFFs not only enhance PINNs' accuracy and efficiency but also improve interpretability, laying the ground for more robust and informative physics-informed learning.

---

## 论文详细总结（自动生成）

这是一份关于论文《Enhancing Physics-Informed Neural Networks with Domain-aware Fourier Features: Towards Improved Performance and Interpretable Results》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：物理信息神经网络（PINNs）虽然在解决偏微分方程（PDEs）方面表现出色，但面临两大挑战：
    1.  **训练困难**：由于损失函数包含 PDE 残差、边界条件（BC）和初始条件（IC）等多项，存在梯度不平衡（梯度硬度）和频谱偏差（倾向于学习低频解）的问题。
    2.  **缺乏解释性**：PINN 通常被视为黑盒模型，难以理解其推理过程以及输入坐标（空间/时间）对预测结果的物理贡献。
*   **核心目标**：提出一种新的输入编码方式和解释性框架，以同时提升 PINN 的数值精度、收敛速度和物理可解释性。

### 2. 论文提出的方法论
*   **核心思想**：引入**领域感知傅里叶特征（Domain-aware Fourier Features, DaFFs）**。与随机傅里叶特征（RFFs）不同，DaFFs 是基于特定领域几何形状和边界条件的拉普拉斯算子特征值分解生成的。
*   **关键技术细节**：
    *   **DaFFs 构造**：通过求解拉普拉斯算子的特征方程 $-\nabla^2 \phi_j = \lambda_j \phi_j$（满足特定边界条件），获得基函数 $\phi_j$。对于矩形等简单领域，这些基函数有闭式解（如正弦函数的组合）。
    *   **硬约束集成**：由于 DaFFs 基函数本身在边界处为零，通过在网络架构中移除偏置项（Bias），可以使模型天然满足齐次边界条件。
    *   **单目标优化**：由于边界条件已通过架构保证，损失函数简化为仅包含 PDE 残差项，消除了复杂的损失平衡权重（Loss Balancing）需求。
    *   **LRP 解释性框架**：开发了专为 PINN 定制的**逐层相关性传播（LRP-ϵ）**算法，并针对残差跳跃连接（Skip Connections）改进了相关性分配规则，用于提取输入特征的贡献分数（Attribution Scores）。

### 3. 实验设计
*   **实验场景**：
    1.  **Kirchhoff-Love 方程**：描述板弯曲问题的四阶 PDE，涉及复杂的边界导数。
    2.  **Helmholtz 方程**：描述波动问题的二阶 PDE，具有明显的频率特性。
*   **Benchmark 与对比方法**：
    *   **Vanilla PINN**：原始的全连接前馈神经网络。
    *   **PINN-RFFs**：使用随机傅里叶特征作为位置编码的 PINN。
    *   **PINN-DaFFs**（本文方法）：使用领域感知傅里叶特征。
*   **评估指标**：训练损失（PDE 残差）、验证损失（与解析解的 MSE）、收敛速度、LRP 归因图的物理一致性。

### 4. 资源与算力
*   **训练时长**：文中详细记录了各模型的训练时间。例如，在 Kirchhoff 问题中，PINN-DaFFs 耗时约 45 分钟；在 Helmholtz 问题中耗时约 31 分钟。
*   **硬件信息**：论文**未明确说明**具体的 GPU 或 CPU 型号及数量。
*   **优化算法**：使用了 Adam 优化器结合 BFGS 算法进行精细调节，训练周期为 50,000 epoch。

### 5. 实验数量与充分性
*   **实验规模**：针对两个复杂的物理问题进行了广泛的超参数搜索（Random Search），涵盖了层数、神经元数、批大小、学习率以及傅里叶特征的特定参数（如 RFF 的方差、DaFF 的模态数）。
*   **充分性评价**：实验设计较为充分。作者不仅对比了最终精度，还展示了完整的学习曲线和损失权重演化过程。通过 XAI（可解释人工智能）分析，从定性和定量两个维度验证了模型捕获物理规律的能力，实验结果具有较强的说服力。

### 6. 论文的主要结论与发现
*   **性能飞跃**：PINN-DaFFs 的验证误差比 Vanilla PINN 和 PINN-RFFs 低出**数个数量级**（在某些案例中达到 $10^{-18}$ vs $10^{-6}$）。
*   **训练效率**：由于简化为单目标优化，PINN-DaFFs 避免了梯度硬度问题，收敛速度显著快于对比方法。
*   **物理一致性**：LRP 分析表明，DaFFs 引导模型关注与物理领域相关的频谱组件，而其他方法产生的特征归因较为破碎且缺乏物理含义。
*   **特征选择指导**：证明了 LRP 归因分数可以作为特征选择的工具，帮助识别哪些频率组件对解的贡献最大。

### 7. 优点
*   **架构创新**：将物理边界条件从“损失函数约束”转变为“网络架构约束”，从根本上简化了优化难题。
*   **理论与解释结合**：不仅提升了精度，还通过 LRP 框架填补了 PINN 在可解释性研究上的空白。
*   **鲁棒性**：DaFFs 消除了 RFFs 中的随机性，提高了模型的可重复性。

### 8. 不足与局限
*   **几何形状限制**：目前 DaFFs 的闭式解主要适用于规则几何（如矩形、圆形）。对于极度复杂的几何形状，需要预先进行数值特征分解（如使用有限差分法），这会增加预处理成本。
*   **非齐次边界条件**：虽然文中提到可以扩展到非齐次情况，但核心实验主要集中在齐次边界条件下，对于更复杂的非齐次 Robin 边界条件的普适性仍需更多验证。
*   **硬件描述缺失**：缺乏具体的算力资源描述，不便于其他研究者评估计算效率的绝对基准。

（完）

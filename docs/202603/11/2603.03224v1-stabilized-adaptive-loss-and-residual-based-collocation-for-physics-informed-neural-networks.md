---
title: Stabilized Adaptive Loss and Residual-Based Collocation for Physics-Informed Neural Networks
title_zh: 物理信息神经网络的稳定自适应损失与基于残差的配置点方法
authors: "Divyavardhan Singh, Shubham Kamble, Dimple Sonone, Kishor Upla"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03224v1"
tags: ["query:wind-wind-control"]
score: 9.0
evidence: 针对PINN的自适应损失平衡方案，用于解决高刚性物理方程
tldr: 本研究针对物理信息神经网络（PINNs）在处理高刚性或激波主导动力学问题时存在的训练不平衡和精度不足问题，提出了两种改进策略：一种是基于平滑梯度范数的自适应损失平衡方案，用于确保初始和边界条件的满足；另一种是自适应残差配置方案，旨在提高高残差区域的求解精度。在Burgers方程和Allen-Cahn方程的测试中，该方法显著降低了误差，提升了PINNs在复杂物理场景下的鲁棒性和准确性。
selection_source: fresh_fetch
motivation: 传统PINNs在处理具有高刚性或激波特征的偏微分方程时，常面临训练不平衡和解精度低的问题。
method: 提出了基于平滑梯度范数的自适应损失平衡方案以及针对高残差区域的自适应残差配置方法。
result: "在Burgers方程和Allen-Cahn方程上，相对L2误差分别降低了约44%和70%。"
conclusion: 该方法通过优化损失权重和采样策略，有效解决了PINNs的训练稳定性问题，并显著提升了复杂物理问题的求解精度。
---

## 摘要
物理信息神经网络（PINNs）已被公认为一种融合了物理信息的求解偏微分方程的无网格替代方法。然而，在处理具有高刚性或激波主导动力学特征的问题时，传统 PINNs 被发现存在局限性，包括训练不平衡和解的不准确，即使物理残差很小也是如此。在本研究中，我们以低粘性粘性 Burgers 方程和 Allen-Cahn 方程作为测试问题，旨在解决这些局限性。为了解决训练不平衡问题，我们开发了一种利用平滑梯度范数的新型自适应损失平衡方案，以确保满足初始和边界条件。此外，为了解决解的不准确性，我们开发了一种基于残差的自适应配置点方案，以提高高物理残差区域的解精度。所提出的新方法在持续满足物理残差的同时，显著提高了求解精度。例如，在 Burgers 方程的情况下，与传统 PINNs 相比，相对 L2 误差降低了约 44%，而对于 Allen-Cahn 方程，相对 L2 误差降低了约 70%。此外，我们利用鲁棒的有限差分解法器展示了所提方法可靠的解对比。

## Abstract
Physics-Informed Neural Networks (PINNs) have been recognized as a mesh-free alternative to solve partial differential equations where physics information is incorporated. However, in dealing with problems characterized by high stiffness or shock-dominated dynamics, traditional PINNs have been found to have limitations, including unbalanced training and inaccuracy in solution, even with small physics residuals. In this research, we seek to address these limitations using the viscous Burgers' equation with low viscosity and the Allen-Cahn equation as test problems. In addressing unbalanced training, we have developed a new adaptive loss balancing scheme using smoothed gradient norms to ensure satisfaction of initial and boundary conditions. Further, to address inaccuracy in the solution, we have developed an adaptive residual-based collocation scheme to improve the accuracy of solutions in the regions with high physics residuals. The proposed new approach significantly improves solution accuracy with consistent satisfaction of physics residuals. For instance, in the case of Burgers' equation, the relative L2 error is reduced by about 44 percent compared to traditional PINNs, while for the Allen-Cahn equation, the relative L2 error is reduced by approximately 70 percent. Additionally, we show the trustworthy solution comparison of the proposed method using a robust finite difference solver.

---

## 论文详细总结（自动生成）

这是一份关于论文《Stabilized Adaptive Loss and Residual-Based Collocation for Physics-Informed Neural Networks》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
物理信息神经网络（PINNs）在求解偏微分方程（PDEs）时具有无网格、易于集成物理约束的优势。然而，在处理**高刚性（High Stiffness）**或**激波主导（Shock-dominated）**的动力学问题（如低粘性流体）时，传统 PINNs 经常失效。
*   **核心矛盾**：研究发现，即使物理残差（Residual）被优化到极小值，PINNs 得到的解在全局范围内仍可能完全错误。
*   **失效原因**：
    1.  **梯度不平衡**：损失函数中 PDE 残差项、初始条件（IC）项和边界条件（BC）项的梯度量级差异巨大，导致优化器忽略某些关键约束。
    2.  **采样效率低下**：均匀分布的配置点无法捕捉解在局部区域（如激波前沿或陡峭梯度区）的剧烈变化。

### 2. 方法论
论文提出了一种结合“优化稳定性”与“采样自适应性”的统一框架，包含两个核心组件：

*   **稳定自适应损失平衡（Stabilized Adaptive Loss Balancing）**：
    *   **核心思想**：利用指数平滑的梯度范数动态调整各项损失的权重。
    *   **技术细节**：计算各项损失相对于网络参数的梯度范数 $g_k$，并进行指数平滑处理：$\bar{g}_k^{(n)} = \beta \bar{g}_k^{(n-1)} + (1-\beta)g_k^{(n)}$。
    *   **权重更新**：权重 $w_k$ 与平滑梯度范数成反比，即 $w_k \propto (\bar{g}_k + \epsilon)^{-\alpha}$。此外，引入了**最小权重下限（Weight Floor）**，防止某一项权重过低导致“权重崩溃”（Weight Collapse）。

*   **基于残差的自适应配置（Residual-Based Adaptive Collocation）**：
    *   **核心思想**：将计算资源集中在物理误差较大的区域。
    *   **流程**：在初始训练阶段后，计算全域的 PDE 残差分布，根据残差大小作为概率分布进行重采样（$p(x,t) \propto |f_\theta(x,t)|$），在高残差区增加配置点，随后进行微调（Fine-tuning）。

### 3. 实验设计
*   **测试场景**：
    1.  **粘性 Burgers 方程**：设置极低粘性系数（$\nu=0.01$），模拟具有陡峭梯度的激波特征。
    2.  **Allen-Cahn 方程**：典型的非线性反应扩散方程，具有亚稳态行为和极薄的相变层。
*   **Benchmark（基准）**：使用基于有限差分法（Finite Difference Method）的鲁棒数值解法器生成的解作为 Ground Truth。
*   **对比方法**：
    *   Standard PINN（固定权重 + 均匀采样）。
    *   Adaptive Loss PINN（仅使用自适应权重）。
    *   **Proposed Method**（Adaptive Loss + Adaptive Collocation）。

### 4. 资源与算力
*   **硬件**：单张 NVIDIA GeForce GTX 1070 GPU。
*   **网络结构**：7 层隐藏层，每层 50 个神经元，激活函数为 $tanh$，总参数量 15,501 个。
*   **训练配置**：使用 Adam 优化器，学习率 $10^{-3}$。
*   **时长/规模**：基础训练 5,000 epoch，自适应采样后的微调阶段增加 2,000 epoch。配置点数量：内部点 10,000，初始/边界点各 2,000。

### 5. 实验数量与充分性
*   **实验组数**：针对两个经典 PDE 问题，分别进行了三组对比实验（标准版、仅权重自适应版、完整改进版）。
*   **充分性评价**：实验设计较为客观，通过消融实验展示了“权重平衡”和“重采样”各自的贡献。引入有限差分法作为基准，避免了 PINN 领域常见的“自说自话”（即仅以残差小作为准确标准）的问题。但实验仅限于一维空间问题，尚未扩展到更高维或更复杂的几何拓扑。

### 6. 主要结论与发现
*   **残差不等于精度**：实验再次证实，极小的 PDE 残差可能对应极大的 L2 误差，证明了单纯优化残差的局限性。
*   **组合效应显著**：
    *   在 Burgers 方程上，相对 L2 误差较标准 PINN 降低了约 **44%**。
    *   在 Allen-Cahn 方程上，相对 L2 误差降低了约 **70%**。
*   **稳定性提升**：平滑梯度权重方案有效缓解了训练初期的梯度病态问题，使边界条件和初始条件的强制执行更加稳健。

### 7. 优点
*   **双管齐下**：同时从优化动力学（损失权重）和空间离散化（配置点分布）两个维度解决 PINN 的失效问题。
*   **鲁棒性改进**：引入指数平滑和权重下限，有效防止了自适应权重方法中常见的“权重崩溃”现象。
*   **验证严谨**：坚持使用传统数值解作为参考，而非仅依赖残差指标，结论更具说服力。

### 8. 不足与局限
*   **权重崩溃风险依然存在**：在 Allen-Cahn 实验中，即便有最小权重限制，PDE 项权重仍一度饱和至 0.99，导致边界误差反而高于标准 PINN，说明对于极度刚性的问题，现有的权重平衡机制仍有改进空间。
*   **计算开销**：自适应重采样需要额外的残差评估和微调步骤，增加了训练的时间成本。
*   **超参数敏感性**：平滑因子 $\beta$、敏感度 $\alpha$ 和权重下限 $w_{min}$ 等超参数可能需要针对不同问题进行手动调优，缺乏完全自动化的普适性。

（完）

---
title: Numerical Approach for On-the-Fly Active Flow Control via Flow Map Learning Method
title_zh: 基于流映射学习方法的实时主动流动控制数值方法
authors: "Xinyu Liu, Qifan Chen, Dongbin Xiu"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07678v1"
tags: ["query:wind-wind-control"]
score: 8.0
evidence: 通过流图学习进行在线主动流动控制
tldr: 本研究提出一种基于流映射学习（FML）的数据驱动数值方法，用于实现即时主动流动控制。该方法通过深度神经网络构建阻力和升力的动力学模型，替代了昂贵的在线流场模拟。通过将FML与深度强化学习（DRL）及模型预测控制（MPC）相结合，在二维圆柱绕流减阻任务中取得了显著成效。该框架不仅显著提高了控制效率，还为其他复杂系统的实时最优控制提供了新思路。
selection_source: fresh_fetch
motivation: 针对主动流动控制中在线数值模拟计算成本高、难以满足实时性要求的问题，探索高效的数据驱动建模方案。
method: 利用流映射学习（FML）构建关于阻力和升力的动力学模型，并将其与DRL和MPC等控制策略集成以实现离线学习与在线控制。
result: "在二维圆柱绕流实验中，该方法在无需在线流场模拟的情况下，成功实现了超过20%的减阻率。"
conclusion: 该研究证明了FML在处理部分观测系统动力学建模方面的有效性，为复杂动力系统的实时最优控制提供了高效且通用的数值方案。
---

## 摘要
我们提出了一种用于实时主动流动控制的数据驱动数值方法，并证明了其在二维不可压缩圆柱绕流减阻中的有效性。该方法基于流映射学习（FML），这是一种近期开发的用于未知动力系统建模的框架，对于部分观测系统尤为有效。针对主动流动控制，我们为感兴趣的物理量（QoIs），即阻力和升力，构建了 FML 动力学模型。在离线学习阶段，生成阻力和升力对控制变量响应的训练数据，并构建基于深度神经网络（DNN）的 FML 模型。学习到的 FML 模型无需进行流场模拟即可实现在线最优流动控制。我们证明了基于 FML 的方法可以与现有的最优控制策略相结合，包括深度强化学习（DRL）和模型预测控制（MPC）。数值结果表明，所提方法能够实现实时流动控制，并实现了超过 20% 的减阻。通过消除控制优化过程中对正向模拟的需求，该方法为其他系统的实时最优控制提供了潜力。

## Abstract
We present a data-driven numerical approach for on-the-fly active flow control and demonstrate its effectiveness for drag reduction in two-dimensional incompressible flow past a cylinder. The method is based on flow map learning (FML), a recently developed framework for modeling unknown dynamical systems that is particularly effective for partially observed systems. For active flow control, we construct an FML dynamical model for the quantities of interest (QoIs), namely the drag and lift forces. During offline learning, training data are generated for the responses of drag and lift to the control variable, and a deep neural network (DNN)-based FML model is constructed. The learned FML model enables online optimal flow control without requiring simulations of the flow field. We demonstrate that the FML-based approach can be integrated with existing optimal control strategies, including deep reinforcement learning (DRL) and model predictive control (MPC). Numerical results show that the proposed approach enables on-the-fly flow control and achieves more than $20\%$ drag reduction. By eliminating the need for forward simulations during control optimization, the approach offers the potential for real-time optimal control in other systems.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种结合流映射学习（Flow Map Learning, FML）与最优控制策略的数值框架，旨在解决复杂流体系统中实时主动流动控制（Active Flow Control, AFC）的高计算成本问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：主动流动控制通常涉及求解非线性偏微分方程（如纳维-斯托克斯方程），其计算成本极高。传统的最优控制方法（如伴随法、MPC）需要反复进行正向模拟，难以满足实时（On-the-fly）控制的需求。
*   **研究动机**：在许多控制任务中，目标函数仅取决于少数物理量（QoIs，如升力、阻力），而非完整的流场状态。如果能直接建模这些 QoIs 的动力学演化，就能将控制优化与昂贵的流场模拟解耦，从而实现极速的实时反馈控制。

### 2. 方法论
*   **核心思想**：利用 **流映射学习 (FML)** 构建一个离散时间算子，直接预测 QoIs（阻力系数 $C_D$ 和升力系数 $C_L$）在控制输入作用下的演化，而无需模拟整个流场。
*   **关键技术细节**：
    *   **FML 模型**：基于 Mori-Zwanzig 形式化理论，针对部分观测系统引入“记忆项”（Memory terms）。模型形式为 $V_{n+1} = G(V_n, \dots, V_{n-n_M}; u_n, \dots, u_{n-n_M})$，其中 $V$ 是 QoIs，$u$ 是控制信号，$n_M$ 是记忆步数。
    *   **深度学习架构**：使用全连接深度神经网络（DNN）来近似算子 $G$。
    *   **控制策略集成**：
        *   **DRL（深度强化学习）**：使用 PPO 算法在 FML 模型构建的“虚拟环境”中训练智能体，避免了在 CFD 环境中训练所需的数百万次模拟。
        *   **MPC（模型预测控制）**：在每个时间步利用 FML 模型进行快速预测和优化，确定最优控制序列。
*   **算法流程**：1. 离线阶段：运行少量高保真 CFD 模拟生成训练数据；2. 训练 FML 模型；3. 在线阶段：将 FML 模型嵌入 DRL 或 MPC 控制器，实时输出控制信号。

### 3. 实验设计
*   **场景**：二维不可压缩圆柱绕流（$Re=100 \sim 500$）。
*   **控制手段**：在圆柱上下两侧设置合成射流（吸气/喷气），通过调节质量流率 $Q(t)$ 进行控制。
*   **实验分类**：
    *   **Type-I**：固定雷诺数（$Re=300$），验证 DRL 和 MPC 的减阻效果。
    *   **Type-II**：未知雷诺数（$Re \in [100, 500]$），验证 FML 模型在参数变化环境下的鲁棒性。
*   **Benchmark**：无控制状态下的标准圆柱绕流（自然涡街脱落状态）。

### 4. 资源与算力
*   **软件工具**：使用开源高阶谱元素求解器 **Nektar++** 进行高保真 CFD 数据生成。
*   **算力说明**：文中**未明确列出**具体的 GPU 型号或 CPU 核心数。
*   **训练细节**：FML 模型训练了 100,000 个 epoch；DRL 训练过程中在 FML 模型上进行了 $10^6$ 次采样。作者强调，由于使用了 FML 替代 CFD，这种规模的采样在传统方法中是不可想象的。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 FML 模型的开环预测精度验证（对比 CFD 真值）。
    *   针对 $Re=300$ 对比了 DRL 和 MPC 的减阻曲线。
    *   针对 Type-II 任务，在 $Re=100, 200, 300, 400, 500$ 以及**超出训练范围**的 $Re=1000$ 进行了闭环控制实验。
*   **充分性评价**：实验设计较为充分，不仅验证了控制效果，还通过跨雷诺数实验证明了模型的泛化能力。对比了两种主流控制策略，增强了结论的说服力。

### 6. 主要结论与发现
*   **减阻效果显著**：该方法在 $Re=300$ 时实现了约 **20%~30%** 的阻力降低。
*   **实时性**：FML 模型极小（仅为 2 维动力系统），计算延迟几乎为零，真正实现了“即时”控制。
*   **泛化能力**：即使在 $Re=1000$（超出训练区间一倍）的情况下，基于 FML 的 MPC 依然能产生有效的减阻效果，证明了 FML 捕捉底层物理规律的能力。

### 7. 优点
*   **非侵入性**：不需要修改底层的 CFD 求解器，仅需输入输出数据。
*   **高效解耦**：将昂贵的偏微分方程求解与频繁的控制优化解耦，大幅降低在线计算开销。
*   **处理部分观测**：通过记忆项成功解决了仅观测 QoIs 时的动力学闭合问题。
*   **灵活性**：可以无缝集成 DRL、MPC 等多种控制框架。

### 8. 不足与局限
*   **离线成本**：虽然在线控制极快，但离线阶段仍需要运行一定数量的高保真 CFD 模拟来获取训练数据。
*   **维度限制**：目前仅在二维简单几何体（圆柱）上验证，在三维复杂几何体或超高雷诺数湍流中的表现尚待观察。
*   **控制手段单一**：仅使用了射流控制，未探讨多种执行器协同工作的复杂场景。

（完）

---
title: "Meta-PINNs: Meta-Learning Enhanced Physics-Informed Machine Learning Framework for Turbomachinery Flow Predictions under Varying Operation Conditions"
title_zh: Meta-PINNs：用于变工况下透平机械流动预测的元学习增强物理信息机器学习框架
authors: "Yuling Han, Zhihui Li, Zhibin Yu"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07740v1"
tags: ["query:wind-wind-control"]
score: 8.0
evidence: 用于流体动力学和流量预测的元学习增强型PINN
tldr: "针对传统物理信息神经网络（PINN）在流体力学中收敛慢、泛化性差的问题，本文提出Meta-PINNs框架。该框架将元学习优化策略引入PINN训练，使其能自动适应不同物理工况。在圆柱绕流和压气机叶栅流动的测试中，Meta-PINNs在精度上比传统方法提高1-2个数量级，计算成本降低90%以上，展现了卓越的泛化能力和训练效率，为复杂透平机械流场模拟提供了新途径。"
selection_source: fresh_fetch
motivation: 旨在解决传统PINN在处理变工况流体动力学问题时存在的收敛缓慢、训练不稳定及泛化能力受限等核心挑战。
method: 提出Meta-PINNs框架，通过集成元优化策略使模型能够自动调整学习过程，以适应不同的物理机制和流动工况。
result: "在圆柱绕流和叶栅流场预测中，该方法精度提升1-2个数量级，且计算成本较传统方法分别降低了95.7%和92.1%。"
conclusion: Meta-PINNs显著提升了复杂透平机械流场模拟的收敛性和泛化性能，为实现高效、智能的流体仿真提供了有力支持。
---

## 摘要
将物理学与机器学习模型耦合在解决由偏微分方程控制的流体动力学问题方面展现出巨大潜力。然而，传统方法（如物理信息神经网络）通常面临收敛慢、训练不稳定以及在不同流动条件下泛化能力有限的问题。为克服这些挑战，本研究提出了一种新型的元学习增强物理信息神经网络（Meta-PINNs）框架，该框架将元优化策略集成到训练过程中。该方法允许模型自动调整其学习过程以适应不同的物理机制，从而显著提高训练效率和预测鲁棒性。所提出的 Meta-PINNs 模型在两个代表性流动问题上进行了评估：(1) 多个入口雷诺数下的圆柱绕流非定常流动，以及 (2) 不同攻角下的压气机叶栅通道内定常湍流。在这两种情况下，通过预测训练集中未包含的雷诺数和攻角下的流场，全面测试了所开发框架的外推性能。结果表明，Meta-PINNs 的精度比原始物理信息神经网络和标准神经网络提高了一到两个数量级，同时计算成本分别降低了高达 95.7% 和 92.1%。它成功捕捉了未见工况下压力和速度分布等关键流动特征的序列模式。因此，研究结果证实 Meta-PINNs 框架在收敛性和泛化性方面较现有机器学习方法有显著提升，为复杂透平机械流动的智能模拟提供了一条极具前景的途径。

## Abstract
Coupling physics with machine learning models has shown great potential for solving fluid dynamics problems governed by partial differential equations. However, conventional methods, such as physics-informed neural networks, often suffer from slow convergence, unstable training, and limited generalization across different flow conditions. To overcome these challenges, this study proposes a novel meta-learning en- hanced physics-informed neural networks (Meta-PINNs) framework, which integrates a meta-optimization strategy into the training process. The approach allows the model to automatically adapt its learning process to varying physical regimes, thereby substantially improving both training efficiency and predictive robustness. The proposed Meta-PINNs model is evaluated on two representative flow problems: (1) unsteady flow around a circular cylinder at multiple inlet Reynolds numbers, and (2) steady turbulent flow within a compressor cascade passage at various angles of attack. In both cases, the extrapolation performance of the developed framework is comprehensively tested by predicting the flow fields at Reynolds numbers and angles of attack that are not included in the training set. The results demonstrate that Meta-PINNs achieve a 1-2 order-of-magnitude improvement in accuracy over vanilla physics-informed neural networks and standard neural networks, while reducing computational cost by up to 95.7 % and 92.1 %, respectively. It successfully captures the sequential patterns of key flow features such as pressure and velocity distributions under unseen conditions. Thus, the findings confirm that the Meta-PINNs framework offers a notable improvement in convergence and generalization over existing machine learning approaches, providing a promising pathway toward smart simulations of complex turbomachinery flows.

---

## 论文详细总结（自动生成）

这是一份关于论文《Meta-PINNs: Meta-Learning Enhanced Physics-Informed Machine Learning Framework for Turbomachinery Flow Predictions under Varying Operation Conditions》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
透平机械（如航空发动机、燃气轮机）的流场预测对于设计优化至关重要。然而，传统计算流体力学（CFD）方法在处理几何变分或多工况迭代设计时计算成本极高。
*   **现有方法的局限**：
    *   **纯数据驱动模型**：在训练分布外（外推）表现差，容易产生物理上不一致的结果。
    *   **传统物理信息神经网络（PINNs）**：面临梯度病态导致的收敛困难，且针对每种新工况（如改变边界条件）都需要重新训练，缺乏灵活性。
*   **研究目标**：开发一种能够快速适应不同操作工况（变雷诺数、变攻角）、兼具物理一致性和高计算效率的流场预测框架。

### 2. 方法论：Meta-PINNs 框架
核心思想是将**元学习（Meta-Learning）**的“学习如何学习”策略引入 PINN 中，通过双层优化机制获取一组优异的初始参数。
*   **核心流程**：
    1.  **任务构建**：将每个特定工况（如特定的 $Re$ 或 $\alpha$）视为一个任务 $T_i$，包含支持集（用于快速适应）和查询集（用于评估元目标）。
    2.  **内循环（Inner Loop）**：针对特定任务，利用少量梯度步长更新模型参数 $\theta$，使其适应当前物理约束和数据。
    3.  **外循环（Outer Loop）**：汇总所有任务的损失，更新全局初始参数 $\theta^*$。
*   **物理约束**：在损失函数中嵌入不可压缩 Navier-Stokes 方程（连续性方程和动量方程）。对于湍流，引入了 Spalart-Allmaras (SA) 一方程模型来处理有效粘度。
*   **关键技术**：利用自动微分计算偏微分方程（PDE）残差，并采用自适应权重系数平衡数据损失和物理损失。

### 3. 实验设计
研究针对两个代表性场景验证了框架的性能：
*   **场景 A：非定常圆柱绕流**
    *   **变量**：入口雷诺数 $Re$。
    *   **数据集**：训练集 $Re \in \{200, 210, 220, 230, 240, 250\}$；测试集（外推）$Re \in \{260, 300\}$。
*   **场景 B：定常压气机叶栅流动（湍流）**
    *   **变量**：攻角 $\alpha$。
    *   **数据集**：训练集 $\alpha \in \{0^\circ, 1^\circ, 2^\circ, 3^\circ, 4^\circ, 5^\circ\}$；测试集包含插值（如 $0.5^\circ$）和外推（$6^\circ$ 至 $10^\circ$）。
*   **Benchmark 与对比方法**：
    *   **基准数据**：使用 SU2 求解器生成的 CFD 高保真数据（已通过实验验证）。
    *   **对比模型**：原始 PINNs（Vanilla PINNs）和标准纯数据驱动神经网络（Standard NNs）。

### 4. 资源与算力
*   **硬件平台**：利物浦大学的 Barkla 高性能计算平台。
*   **GPU 型号**：1 块 **NVIDIA H100** GPU。
*   **训练时长**：
    *   圆柱流案例：Meta-PINNs 耗时约 **7 小时 59 分**（相比之下，传统 PINN 需约 7 天 18 小时）。
    *   叶栅流案例：Meta-PINNs 耗时约 **8 小时 58 分**（相比之下，传统 PINN 需约 28 小时 41 分）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从层流到湍流、从定常到非定常的两种复杂流动模式。
*   **充分性**：
    *   **多维度评估**：不仅对比了速度、压力、粘度等全流场分布，还对比了 Strouhal 数（$St$）和总压损失系数（$\xi$）等关键工程指标。
    *   **泛化性测试**：专门设置了外推实验（测试集完全在训练范围之外），这是衡量模型鲁棒性的严苛标准。
    *   **公平性**：所有对比模型（Meta-PINNs, PINNs, NNs）均采用相同的网络架构（6 层隐藏层，每层 128 个神经元），确保了性能差异仅源于学习策略。

### 6. 主要结论与发现
*   **精度大幅提升**：Meta-PINNs 的预测精度（RMSE）比传统 PINNs 和 NNs 提高了一到两个数量级。
*   **计算成本骤降**：在圆柱流案例中，计算成本降低了 **95.7%**（对比 PINN）；在叶栅流案例中降低了 **45.6%**。
*   **卓越的外推能力**：即使在训练范围之外的工况下（如 $Re=300$ 或 $\alpha=10^\circ$），Meta-PINNs 仍能准确捕捉卡门涡街的演化和叶片表面的压力分布，误差保持在极低水平（如总压损失误差 < 3.8%）。
*   **快速适应性**：元学习提供的初始化参数使得模型仅需极少量的迭代即可收敛至新工况。

### 7. 优点与亮点
*   **解决“冷启动”问题**：通过元学习解决了 PINN 在新工况下必须从头训练的痛点。
*   **物理-数据双驱动**：结合了物理方程的严谨性和元学习的灵活性，有效缓解了 PINN 的梯度病态问题。
*   **工程实用性强**：首次将 Meta-PINNs 应用于复杂的透平机械叶栅湍流预测，证明了其在工业级应用中的潜力。

### 8. 不足与局限
*   **维度限制**：目前研究仅限于 **2D** 流场，尚未在更复杂的 3D 真实透平机械几何结构中验证。
*   **外推边界**：虽然外推表现优异，但随着工况偏离训练集越远（如极高雷诺数或极大攻角导致的严重失速），预测误差仍呈现单调上升趋势。
*   **湍流模型简化**：使用了 SA 简化的湍流模型，对于涉及强烈分离或二次流的超复杂流动，其物理约束的准确性可能受限。

（完）
